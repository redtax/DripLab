<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <!-- Android 11+ package visibility: 声明 TTS 引擎可见性 -->
    <queries>
        <intent>
            <action android:name="android.intent.action.TTS_SERVICE" />
        </intent>
        <!-- 推荐的 TTS 引擎包名显式声明 -->
        <package android:name="com.google.android.tts" />
        <package android:name="com.xiaomi.mibrain.speech" />
        <package android:name="com.iflytek.vflynote" />
        <package android:name="com.iflytek.voicenote" />
        <package android:name="com.iflytek.tts" />
        <package android:name="com.iflytek.cloud" />
        <package android:name="com.iflytek.speechcloud" />
    </queries>

    <application
        android:name=".DripLabApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="滴落间Lab"
        android:supportsRtl="true"
        android:theme="@style/Theme.DripLab">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.DripLab">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>
</manifest>